import React from 'react';
import './Spinner.scss';

const spinner = () => (
    <div className='par'>
        <div id="main">
            <span className="spinner"></span>
        </div>
    </div>

);

export default spinner;